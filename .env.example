# Server
NODE_ENV=development
PORT=5000

# Database (PostgreSQL / Neon)
# Example (Neon): postgres://user:password@ep-xxxxx-xxxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# Toggle real DB storage (true to use Drizzle+Neon; false for in-memory mocks)
USE_DB=false

# Email (SMTP) used by contact form
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
